# LinkedIn campaign scheduling receipts

This directory contains credential-free receipts created only after an exactly approved LinkedIn campaign has been scheduled through the user's signed-in browser and visibly reconciled against LinkedIn's scheduled-post ledger.

Receipts may retain campaign identifiers, public settings, scheduled times, repository asset paths, and content hashes. They must never retain private scheduler URLs, credentials, cookies, browser state, or signed-in session data.
