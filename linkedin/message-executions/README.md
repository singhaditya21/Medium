# LinkedIn private-message execution receipts

This directory contains credential-free receipts created only after one exact, individually approved LinkedIn message has been sent and visibly verified.

Receipts store the recipient's public profile URL and a SHA-256 digest of the approved message. They never store message text, conversation history, private conversation URLs, credentials, cookies, or browser state.

Record a verified message with `scripts/record_linkedin_message.py`.
